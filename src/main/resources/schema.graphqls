type Query {
    listMessages: [Chat!]!
}

type Mutation {
    createMessage(message: String!): Message!
}

type Subscription {
    updateMessage(id: ID!): Message
}

type Message {
    id: ID!
    message: String!
}
