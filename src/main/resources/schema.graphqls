type Query {
    listChat: [Chat!]!
}

type Mutation {
    createChat(chat: String!): Chat!
    updateChat(id: ID!, chat: String!): Chat
    deleteChat(id: ID!): Boolean
}

type Subscription {
    updateChat(id: ID!): Chat
}

type Chat {
    id: ID!
    chat: String!
}
