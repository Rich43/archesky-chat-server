type Query {
    listMessages: [Chat!]!
}

type Mutation {
    createChat(chat: String!): Message!
}

type Subscription {
    updateMessage(id: ID!): Message
}

type Message {
    id: ID!
    message: String!
}
