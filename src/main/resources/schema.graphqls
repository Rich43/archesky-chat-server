type Query {
    listMessages: [Message!]!
}

type Mutation {
    createMessage(message: String!): Message!
}

type Subscription {
    updateMessage(id: ID!): Message
}

type Message {
    id: ID!
    message: String!
}
